# README

## i18n

Project uses react-i18next and i18next-cli for translations.

manually run
`npm run i18n:extract` to generate translation keys in `locales/lng/ns` and typescript types.

or run `npm run i18n:watch` to watch in development for changes and
automatically generate translation keys in `locales/lng/ns` and typescript types.
